import { TypesToArrayPipe } from './types-to-array.pipe';

describe('TypesToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TypesToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
